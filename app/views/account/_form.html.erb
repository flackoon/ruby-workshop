<%= form_with(model: user, local: true) do |form| %>
  <div class="card card-primary p-3">
    <div class="form-group">
      <%= form.label :email %>
      <%= form.text_field :email, class: 'form-control' %>
      <div class='text-danger'><small><%= @user.errors[:email].map{ |error| error.capitalize }.join('</br>') %></small></div>
    </div>
    <div class="form-group">
      <%= form.label :password %>
      <%= form.password_field :password, class: 'form-control' %>
      <div class='text-danger'><small><%= @user.errors[:password].map{ |error| error.capitalize }.join('</br>') %></small></div>
    </div>
    <div class="form-group">
      <%= form.label :password_confirmation %>
      <%= form.password_field :password_confirmation, class: 'form-control' %>
      <div class='text-danger'><small><%= @user.errors[:password_confirmation].map{ |error| error.capitalize }.join('</br>') %></small></div>
    </div>
  </div>
  <div class="actions text-right mt-3">
    <%= form.submit class: 'btn btn-outline-primary' %>
  </div>
<% end %>



